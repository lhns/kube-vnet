kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: kube-vnet-e2e
networking:
  # Disable kindnet so we can install Calico as the CNI.
  disableDefaultCNI: true
nodes:
  - role: control-plane
  - role: worker
