{{- /*
CRD shipped as a templated resource (not under `charts/kube-vnet/crds/`) so
`helm upgrade` updates it on chart bumps. Helm's special crds/ directory is
install-once-only and does not propagate updates -- see
https://helm.sh/docs/chart_best_practices/custom_resource_definitions/.
The `helm.sh/resource-policy: keep` annotation prevents `helm uninstall`
from cascading-deleting every existing CR alongside the CRD. ADR 0030.

Generated from config/crd/bases/ by `make manifests`
(scripts/gen-chart-crds.sh). Do not edit by hand -- edit the API types under
api/v1alpha1/ and regenerate. A stale copy breaks `helm install`.
*/ -}}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
    helm.sh/resource-policy: keep
  name: virtualnetworks.kube-vnet.lhns.de
spec:
  group: kube-vnet.lhns.de
  names:
    kind: VirtualNetwork
    listKind: VirtualNetworkList
    plural: virtualnetworks
    shortNames:
    - vnet
    - vnets
    singular: virtualnetwork
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VirtualNetwork is a named virtual network. Pods that share a VirtualNetwork
          can communicate; pods on different (or no) VirtualNetworks are isolated by
          the operator-managed NetworkPolicy set. A VirtualNetwork lives in one
          "home" namespace and may permit pods from other namespaces to join via
          spec.allowedNamespaces.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualNetworkSpec is the desired state of a VirtualNetwork.
            properties:
              allowedNamespaces:
                description: |-
                  AllowedNamespaces controls which namespaces' pods may join this VirtualNetwork.
                  The home namespace is always allowed; if this field is unset, only the home
                  namespace is allowed.
                properties:
                  all:
                    description: |-
                      All allows pods from any namespace to join. This is the wildcard form.
                      When true, Names and Selector are ignored.
                    type: boolean
                  names:
                    description: |-
                      Names is an explicit list of namespace names allowed to join.
                      Names are matched exactly — no glob/wildcard patterns. Use Selector
                      for label-based grouping; use All for "any namespace".
                    items:
                      type: string
                    type: array
                  selector:
                    description: Selector matches namespaces by labels.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              description:
                description: Description is free text. Not interpreted by the operator.
                type: string
            type: object
          status:
            description: VirtualNetworkStatus is the observed state of a VirtualNetwork.
            properties:
              conditions:
                description: |-
                  Conditions follow the standard Kubernetes condition pattern.
                  Known types: Ready, Degraded.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              generatedPolicies:
                description: GeneratedPolicies lists NetworkPolicy resources owned
                  by this VirtualNetwork.
                items:
                  description: PolicyRef references a generated NetworkPolicy.
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              members:
                description: Members is the observed pod membership grouped by namespace.
                items:
                  description: NamespaceMembers groups observed pod members by namespace.
                  properties:
                    namespace:
                      type: string
                    pods:
                      items:
                        type: string
                      type: array
                  required:
                  - namespace
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
        x-kubernetes-validations:
        - message: VirtualNetwork name must be a DNS-1123 label (lowercase alphanumeric
            and hyphens; no dots)
          rule: self.metadata.name.matches('^[a-z0-9]([-a-z0-9]*[a-z0-9])?$')
    served: true
    storage: true
    subresources:
      status: {}
