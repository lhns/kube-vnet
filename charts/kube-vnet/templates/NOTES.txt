kube-vnet is installed.

The VirtualNetwork CRD is registered. Define a network and label pods to join it:

  kubectl get vnet -A
  kubectl get networkpolicy -A -l kube-vnet/managed-by=kube-vnet

Sample manifests are in the project repo at
https://github.com/lhns/kube-vnet/tree/main/config/samples

{{- $iso := default (dict) .Values.operator.ingressIsolation }}
{{- $mode := default "none" $iso.mode }}
{{- if eq $mode "pod" }}

WARNING: ingressIsolation.mode is "pod". Every non-excluded, non-disabled
namespace will get the default-deny baseline (no ingress allowed except via
vnet membership policies). Workloads that previously relied on default-allow
will lose ingress connectivity from outside their vnet.

{{- end }}
{{- if .Values.operator.defaultDenyEverywhere }}

DEPRECATED: operator.defaultDenyEverywhere is set. Switch to
operator.ingressIsolation.mode (set to "pod" for the same effect). The old
value will be removed in a future release.

{{- end }}
